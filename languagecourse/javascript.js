/*

 function validateForm() {
        let vorname = document.getElementById("vorname").value; 

        sessionStorage.setItem("vorname", vorname); //wert speichern im Memory des Browsers
        alert("vorname = "+vorname);
    }

        //document.getElementById("vorname").innerHTML; // das nimmt den wert aus dem cache und setzte sie als id = vorname Die Eigenschaft Element.innerHTML liest und speichert den Inhalt eines HTML-Elements. Wenn Sie beim dynamischen Ändern des gespeicherten Inhalts HTML-Tags notieren, werden diese bei der Aktualisierung des Elementinhalts interpretiert. */